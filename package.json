{
  "name": "saket-portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Saket Mundhada portfolio site with an Ask-Saket AI chatbot (OpenAI tool-calling agent).",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "node --env-file=backend/.env backend/server.js",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "engines": {
    "node": ">=18"
  }
}
